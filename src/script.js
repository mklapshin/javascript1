import {pow} from './pow'

export const plus = (a, b) => {
  return a + b;
};

export const minus = (a, b) => {
  return a - b;
};

export default plus;

console.log('Something')

console.log(pow(3,5))
